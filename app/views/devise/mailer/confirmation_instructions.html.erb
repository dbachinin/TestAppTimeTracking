<p>Welcome <%= @email %>!</p>

<p>You can confirm your account name <%= User.find_by(email: @email).name%> through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>
