<%= form_with url: ([@user,@project]) do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :project_name %>
    <%= form.text_field :project_name, id: :project_project_name %>
  </div>

  <div class="field">
    <%= form.label :task %>
    <%= form.text_field :task, id: :project_tasks %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :project_description %>
  </div>
<%= form.hidden_field :user_id, value: current_user.id %>
<%= form.hidden_field :uid, value: SecureRandom.uuid %>
<%= form.hidden_field :user, value: @user %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
