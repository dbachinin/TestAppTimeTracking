<h1>New Task</h1>

<%= render 'form', task: @task %>
<ul class="breadcrumb" >
			<li class="breadcrumb-item">
				<%= link_to "Projects", user_projects_path(@user, @project) %>
			</li>
			<li class="breadcrumb-item">
				<a href="#"><%= link_to @project.project_name, project_path(@project) %></a>
			</li>
		</ul>
<%= link_to 'Back', project_tasks_path(@project) %>
